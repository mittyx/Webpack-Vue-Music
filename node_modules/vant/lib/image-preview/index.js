'use strict';

exports.__esModule = true;

var _vue = require('vue');

var _vue2 = _interopRequireDefault(_vue);

var _imagePreview = require('./image-preview');

var _imagePreview2 = _interopRequireDefault(_imagePreview);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var instance = void 0;

var initInstance = function initInstance() {
  instance = new (_vue2.default.extend(_imagePreview2.default))({
    el: document.createElement('div')
  });
  document.body.appendChild(instance.$el);
};

var ImagePreview = function ImagePreview(images, startPosition) {
  if (!instance) {
    initInstance();
  }

  var config = Array.isArray(images) ? { images: images, startPosition: startPosition } : images;

  instance.images = config.images;
  instance.startPosition = config.startPosition || 0;
  instance.value = true;
  instance.$on('input', function (show) {
    instance.value = show;
    if (!show) {
      instance.$off('input');
      config.onClose && config.onClose();
    }
  });

  return instance;
};

ImagePreview.install = function () {
  _vue2.default.use(_imagePreview2.default);
};

exports.default = ImagePreview;